/*package com.example.myapplication
import com.google.gson.Gson

class RememoList {
    val rememo: List<ReminderData> =
        Gson().fromJson(rememos, Array<ReminderData>::class.java).asList()
    val posterTable: MutableMap<String, Int> = mutableMapOf()
    val descriptTable: MutableMap<Int, String> = mutableMapOf()

    init {
// create poster table mapping title and image id


        for (movie in rememo) {
            var g = ""
            //movie.checked = false
        }
    }
}*/
